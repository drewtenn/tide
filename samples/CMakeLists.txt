# Each sample is its own subdirectory and links the minimum it needs.
# Phase 0 scaffold: samples/00_window will land as the deliverable demo.
# Currently empty so CMake configure stays green with no targets.

# add_subdirectory(00_window)
