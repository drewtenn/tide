# Build artifacts
build/
out/
cmake-build-*/
.cache/

# vcpkg
vcpkg_installed/
vcpkg/

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo
CMakeUserPresets.json

# Compile databases
compile_commands.json

# OS
.DS_Store
Thumbs.db
*.swp

# Profiler captures
*.tracy
captures/

# Tools (keep)
!cmake/
!docs/
!engine/
!samples/
!tests/
!.claude-octopus/

# Local Claude Code config — per-machine settings + scheduler lock + transient
# session caches. Keep out of the repo entirely; each developer has their own.
.claude/

# Per-session orchestrator state — rewritten on every workflow run, no value to commit.
.claude-octopus/state.json
.claude-octopus/state.json.backup
