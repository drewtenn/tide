# Unit tests are doctest-based. Phase 0 scaffold: directory exists; first tests
# (Handle ABA reuse, action-map edge cases) land alongside the .h implementations.

# find_package(doctest CONFIG REQUIRED)
# add_subdirectory(unit)
